# Runtime + test dependencies for the CarlaRL model-serving image.
# Pins mirror the resolved versions in uv.lock; keep them in sync with
# pyproject.toml when bumping. Requires Python >=3.12 (numpy 2.5 wheels).
fastapi==0.139.0
uvicorn[standard]==0.50.0
pydantic==2.13.4
python-multipart==0.0.32
pyyaml==6.0.3
toml==0.10.2
prometheus-client==0.25.0
jinja2==3.1.6
watchdog==6.0.0
deepdiff==9.1.0
psutil==7.2.2
torch==2.13.0
numpy==2.5.1
requests==2.34.2
httpx==0.28.1
pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov==7.1.0
