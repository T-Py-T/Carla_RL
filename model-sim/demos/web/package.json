{
  "name": "carla-rl-fsd-web-demo",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "capture": "npm run build && node scripts/capture.mjs",
    "fetch:sketchfab-traffic": "node scripts/sketchfab-fetch.mjs",
    "verify:sketchfab-traffic": "node scripts/sketchfab-verify.mjs"
  },
  "dependencies": {
    "three": "^0.172.0"
  },
  "devDependencies": {
    "@types/three": "^0.172.0",
    "puppeteer": "^24.6.0",
    "typescript": "^5.7.3",
    "vite": "^6.0.7"
  }
}
